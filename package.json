{
  "name": "fs-nextra",
  "version": "0.3.8",
  "description": "Node.js V8 native fs enhanced with util.promisify and standard extra methods.",
  "main": "dist/index.js",
  "scripts": {
    "test": "npx tsc && npx ava test/*.js",
    "test:lint": "npx eslint src test && npx tslint 'src/*.ts'",
    "test:coverage": "npm run coverage && npx nyc check-coverage --lines 60 --functions 60 --branches 60",
    "coverage": "npx nyc npm test",
    "coveralls": "npx nyc report --reporter=text-lcov | npx coveralls",
    "docs": "npx jsdoc -c ./.docstrap.json -R README.md",
    "lint": "npx eslint --fix src test && npx tslint --fix 'src/*.ts'"
  },
  "keywords": [
    "atomics",
    "copy",
    "delete",
    "directory",
    "file",
    "file system",
    "fs",
    "json",
    "move",
    "native",
    "nextra",
    "promise",
    "read",
    "write"
  ],
  "author": "BDISTIN",
  "license": "MIT",
  "engines": {
    "node": ">=8.6.0"
  },
  "devDependencies": {
    "@types/node": "^10.1.3",
    "ava": "^1.0.1",
    "coveralls": "^3.0.2",
    "eslint": "^5.2.0",
    "eslint-config-klasa": "github:dirigeants/klasa-lint",
    "ink-docstrap": "github:bdistin/docstrap",
    "jsdoc": "github:jsdoc3/jsdoc",
    "nyc": "^13.0.1",
    "tslint": "^5.7.0",
    "tsubaki": "^1.2.0",
    "typescript": "^3.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bdistin/fs-nextra"
  }
}
